---

const { isNew, title, date, img, duration } = Astro.props;
---
<div class="w-full bg-[rgb(50,0,80)] flex flex-col cursor-pointer hover:bg-[rgb(60,0,90)] duration-150
        md:flex-row
">
    <div class="relative flex justify-center items-center
        md:flex-1/2
    ">
        <img src={img} class="object-cover">
        <img src="play.svg" class="absolute h-20 p-5 bg-white/30 backdrop-blur-md rounded-full "> 
        <p class="absolute bottom-3  text-white left-3 p-2 tracking-normal text-sm bg-[rgb(50,0,80)]"> {duration} </p>
    </div>
    <div class="ml-4 tracking-normal py-2 flex gap-2 justify-center flex-col items-baseline
        md:flex-1/2 md:py-0
    ">
        {isNew && <p class="text-sm bg-violet-500 px-2 inline"> Nuevo </p>}
        <p class="text-lg text-white uppercase lg:text-xl"> {title} </p>
        <p class="text-sm text-gray-400 lg:text-lg"> {date} </p>
    </div>
</div>
